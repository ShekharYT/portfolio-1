<section id="hero" class="section hero">
  <div class="container hero-layout">
    <div class="hero-copy reveal">
      <p class="eyebrow">Freelance Video Editor</p>
      <h1>I make people stop scrolling.</h1>
      <p class="subhead">Reels, YouTube, and short-form edits designed to hold attention and drive action.</p>
      <p class="badge">1-2 years | Adobe Premiere Pro | After Effects</p>
      <ul class="tool-strip" aria-label="Editing strengths">
        <li>Premiere Pro</li>
        <li>After Effects</li>
        <li>Fast turnaround</li>
      </ul>
      <div class="hero-actions">
        <a class="btn btn-primary" href="https://calendly.com/abhishekprakash0000" target="_blank" rel="noreferrer">Book a Call</a>
        <a class="btn btn-secondary" href="#work">View My Work</a>
      </div>
    </div>
    <div class="hero-visual card reveal">
      <a class="video-still" href="https://youtube.com/shorts/VbM_QNdbnR4?feature=share" target="_blank" rel="noreferrer">
        <img src="https://i.ytimg.com/vi/VbM_QNdbnR4/hq2.jpg" alt="Showreel Preview" class="hero-bg-img" />
        <div class="hero-overlay">
          <span class="play-icon" aria-hidden="true">▶</span>
          <p>Showreel Preview</p>
        </div>
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: calc(var(--space-9) - var(--space-4));
  }

  .hero-layout {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: var(--space-7);
    align-items: center;
  }

  .eyebrow {
    margin: 0 0 var(--space-3);
    color: var(--color-muted);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-size: 0.8rem;
  }

  h1 {
    margin: 0;
    font-family: "DM Serif Display", Georgia, serif;
    font-size: clamp(2.6rem, 7vw, 5rem);
    line-height: 0.98;
    max-width: 12ch;
  }

  .subhead {
    margin: var(--space-4) 0 var(--space-3);
    color: var(--color-muted);
    max-width: 46ch;
    font-size: 1.05rem;
  }

  .badge {
    margin: 0 0 var(--space-4);
    display: inline-flex;
    background: #f0f0eb;
    border-radius: 999px;
    padding: 0.35rem 0.8rem;
    font-weight: 500;
    font-size: 0.9rem;
  }

  .tool-strip {
    list-style: none;
    margin: 0 0 var(--space-5);
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tool-strip li {
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    border-radius: 999px;
    padding: 0.28rem 0.72rem;
    font-size: 0.83rem;
    font-weight: 600;
    color: #4d4d4d;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
  }

  .hero-visual {
    aspect-ratio: 4 / 5;
    overflow: hidden;
    box-shadow: var(--shadow-strong);
  }

  .video-still {
    width: 100%;
    height: 100%;
    display: block;
    position: relative;
    color: #ffffff;
    text-align: center;
    text-decoration: none;
  }

  .hero-bg-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.4s ease;
  }

  .video-still:hover .hero-bg-img {
    transform: scale(1.05);
  }

  .video-still:hover .play-icon {
    transform: scale(1.1);
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    background: linear-gradient(160deg, rgba(26, 26, 26, 0.18), rgba(26, 26, 26, 0.82));
  }

  .play-icon {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    display: inline-grid;
    place-items: center;
    background: color-mix(in srgb, var(--color-accent) 75%, #fff);
    font-size: 1.4rem;
    transition: transform 0.3s ease;
  }

  @media (max-width: 900px) {
    .hero-layout {
      grid-template-columns: 1fr;
    }
  }
</style>
